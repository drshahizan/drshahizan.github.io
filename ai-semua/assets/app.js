document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.prompt').forEach(box=>{if(box.querySelector('.copy-btn'))return;const btn=document.createElement('button');btn.className='copy-btn';btn.type='button';btn.textContent='Salin Prompt';btn.addEventListener('click',async()=>{const clone=box.cloneNode(true);clone.querySelectorAll('button').forEach(x=>x.remove());try{await navigator.clipboard.writeText(clone.textContent.trim());btn.textContent='✓ Sudah disalin';setTimeout(()=>btn.textContent='Salin Prompt',1800)}catch(e){btn.textContent='Pilih & salin teks'}});box.appendChild(btn)});});
