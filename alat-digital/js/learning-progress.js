(()=>{const box=document.querySelector('[data-learning-summary]');if(!box)return;const keys=Object.keys(localStorage).filter(k=>/^kursus-\d+-\d+$/.test(k));const done=keys.filter(k=>localStorage.getItem(k)==='1').length;const total=47;const pct=Math.round(done/total*100);box.innerHTML=`<strong>${done}/${total}</strong><div><p>${done?`${done} latihan telah diselesaikan. Teruskan mengikut kadar anda.`:'Kemajuan disimpan pada pelayar ini apabila latihan ditandakan selesai.'}</p><div class="progress-bar" aria-label="${pct}% selesai"><span style="width:${pct}%"></span></div></div><a class="btn btn-primary btn-small" href="latihan/index.html">${done?'Sambung Belajar':'Mulakan Latihan'} →</a>`;})();
